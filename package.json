{
  "name": "iron-archive-site",
  "type": "module",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "build:fixtures": "IRON_DEV_FIXTURES=1 astro build && pagefind --site dist",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.6.0",
    "fast-glob": "^3.3.2",
    "yaml": "^2.5.1"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
